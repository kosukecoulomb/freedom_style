<% if resource.errors.any? %>
  <h5 class="text-danger"><%= resource.errors.count %>件のエラーが発生しました</h5>
  <ul>
    <% resource.errors.full_messages.each do |message| %>
      <li class="text-danger"><%= message %></li>
    <% end %>
  </ul>
<% end %>