<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2 class="mb-3">マイページ</h2>
    </div>
  </div>

  <div class="row">
    <div class="col-md-3">
      <h4><%= @user.name %>さんの基本データ<%= link_to "登録情報を編集する", edit_profile_path(@user), class: "btn btn-outline-info" %></h4>
      <table class="table mb-5">
        <tr>
          <th>年齢</th>
          <td><%= @user.generation_i18n %></td>
        </tr>
        <tr>
          <th>身長</th>
          <td><%= @user.tall %>cm</td>
        </tr>
        <tr>
          <th>体型</th>
          <td><%= @user.body_shape_i18n %></td>
        </tr>
        <tr>
          <th>足のサイズ</th>
          <td><%= @user.foot_size_i18n %></td>
        </tr>
      </table>
      <h4>コレクション</h4>
      <%= link_to "登録したアイテムの一覧を見る", items_path, class: "btn btn-outline-success" %>
      
      <h4>新しい投稿をする</h4>
      <%= link_to "コーディネート", new_coordinate_path, class: "btn btn-outline-success" %>
      <%= link_to "コレクション", new_item_path, class: "btn btn-outline-success" %>
    </div>

    <div class="col-md-9">
      <h4><%= @user.name %>さんに似たユーザーの投稿</h4>
      <% if @similar_coordinates != nil %>
        <%= render "layouts/coordinates", coordinates: @similar_coordinates %>
      <% else %>
        <P>該当する投稿はありません</P>
      <% end %>
      検索画面のリンク！

      <h4 class="mt-3">いいねした投稿</h4>
      <% if @favorite_coordinates != nil %>
        <%= render "layouts/coordinates", coordinates: @favorite_coordinates %>
        <%= link_to "いいねした全アイテムを見る", favorites_path %>
      <% else %>
        <P>該当する投稿はありません</P>
      <% end %>
      
      <h4 class="mt-3">あなたの投稿</h4>
      <% if @coordinates != nil %>
        <%= render "layouts/coordinates", coordinates: @coordinates %>
        <%= link_to "あなたの全投稿を見る", user_path(current_user) %>
      <% else %>
        <P>該当する投稿はありません</P>
      <% end %>
    </div>
  </div>
</div>
